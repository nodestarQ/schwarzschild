version: '3.8'

services:
  relay:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "8080:8080"
    volumes:
      - .:/app
    env_file:
      - .env
    environment:
      - PORT=8080
    command: air -c .air.toml
